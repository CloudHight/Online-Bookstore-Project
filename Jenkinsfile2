pipeline {
    agent any
    
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Archiving the artifacts'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
        stage ("Deploy to Tomcat"){
            steps {
                sshagent(['jenkins-key']) {
                    sh "scp -o StrictHostKeyChecking=no **/*.war ubuntu@13.36.238.250:/opt/tomcat/webapps"
                    sh 'ssh -t -t ubuntu@13.36.238.250 -o strictHostKeyChecking=no "sudo rm -rf /opt/tomcat/webapps/ROOT && mv /opt/tomcat/webapps/*.war /opt/tomcat/webapps/ROOT.war"'
                }
            }
        }
    }
}
